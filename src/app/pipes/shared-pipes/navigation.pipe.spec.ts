import {NavigationPipe} from './navigation.pipe';

describe('NavigationPipe', () => {
  it('create an instance', () => {
    const pipe = new NavigationPipe();
    expect(pipe).toBeTruthy();
  });
});
